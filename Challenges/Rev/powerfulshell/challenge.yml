# This file represents the base specification of your challenge. It is used by
# other tools to install and deploy your challenge.

# Required sections
name: "Powerful Shell"
author: "fern89"
category: rev
description: Someone sent me this powershell script... can you decrypt it and get the flag?

# Dynamic Scoring Moments
# Value: Initial points
# Decay: How many solves before minimum
# Minimum: Minimum points
value: 500
type: dynamic
decay: 40
minimum: 100

# The flag to use
flags:
    - blahaj{p0w3rFu1L_ShEl1}

# Link all files in dist
files:
    - dist/chal.ps1

# The state of the challenge.
# If the field is omitted, the challenge is visible by default.
# If provided, the field can take one of two values: hidden, visible.
state: visible

# Specifies what version of the challenge specification was used.
# Subject to change until ctfcli v1.0.0
version: "0.1"